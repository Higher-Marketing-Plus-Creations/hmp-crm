<IfModule mod_rewrite.c>
    RewriteEngine On

    # Route the app through /public when the vhost cannot point there directly.
    RewriteCond %{REQUEST_URI} !^/public/
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>
